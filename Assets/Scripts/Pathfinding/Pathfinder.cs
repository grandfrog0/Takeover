using UnityEngine;

public class Pathfinder : MonoBehaviour
{
    // deleteit
    [SerializeField] GridTransform target;



    //deleteit
    private void Start()
    {
        //var x = FindPath(transform.position, target.Position);
        //if (x != null) Debug.Log(string.Join("; ", x));
        //StartCoroutine(MoveTo(x));
    }
}
